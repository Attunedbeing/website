# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Local secrets (Instagram token etc.) — never commit
.env
.env.*

# Meeting recordings (local only, not for the repo)
recordings/

# Deno lockfile (generated by Netlify CLI, not a Deno project)
deno.lock
